# Environment Configuration
ENVIRONMENT=development

# API Configuration
API_BASE_URL=http://localhost:3000/api

# Feature Flags
ENABLE_OFFLINE_MODE=true
ENABLE_ANALYTICS=false
ENABLE_CRASH_REPORTING=false

# Development Configuration
# These settings are only used in development mode
DEV_USE_MOCK_API=false
DEV_LOG_API_CALLS=true
DEV_SHOW_DEBUG_MENU=true

# Build Configuration
# These settings are used during the build process
BUNDLE_ID=com.sbdsa.app
APP_NAME=SBD SA
APP_VERSION=1.0.0
BUILD_NUMBER=1

# External Services
# Add any external API keys or service configurations here